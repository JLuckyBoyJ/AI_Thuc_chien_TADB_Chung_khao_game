export const playSound = (sound: string) => {
  console.log(`Playing sound: ${sound}`);
  // In the future, this will use the Web Audio API to play sounds.
  // For example:
  // const audio = new Audio(`/sounds/${sound}.mp3`);
  // audio.play();
};
